# 参考的结果计划指南.md（终局对标分析报告 · 2026-09-08）

> 状态：**Phase A（ANALYSIS_ONLY）分析完成**。产出 9 节报告，**等待用户批准具体实施批次**。分析期零源码改动（`git status` 确认仅 `workflow_status.md` 与 .gitignore 被外部工具改动；`src/ open-sse/ skills/ tests/ cli/ docs/` 均未动）。

---

## Task Contract
- **原始目标**：参考目录 868 项全部汲取；CL4R1T4S 顶级系统提示词 → 做「更懂用户的 agent + 小白易用 + 黑匣子全开 + 用户 skills 沉淀 + 图片/视频/PPT/电商扩展」；先分析后实施；用户确认批次后再改代码。
- **当前阶段**：Phase A 完成；Phase B 待授权。
- **子代理**：13 个并行已全回（T01 CL4R1T4S、T02 盘点、T03 网关、T04 skills工程、T05 记忆、T06 终端UX、T07 边用边学、T08 媒体、T09 主项目快照、T10 一致性、T11 onboarding、T12 diff审计、T00 基线）。

---

## 1. 项目识别结果

### 主项目
- **类型**：本地/远程 AI 路由网关（Next.js 16 App Router + Node 自定义服务器）+ dashboard + open-sse 引擎 + SQLite 持久化。
- **版本**：v0.5.75（2026-09-08）。入口 `src/app/api/v1/*`（rewrites 映射 `/v1/*`）；鉴权 `src/dashboardGuard.js` deny-by-default；引擎 `open-sse/`（provider/translator/executor/rtk）。
- **技能体系**：内置 9 技能（`src/shared/constants/skills.js`，含 video）+ 用户自定义技能（`kv.userSkills`，`/api/skills` 系列）。

### 参考项目扫描（868 → 15 桶，★32 高价值候选）
依据：目录名语义（65%）+ README 首行/内文（25%）+ 不可归类 8%。分布：AGENT 232 / SKILL-EN 75 / DEV-TECH 64 / SECURITY 49 / MEDIA 41 / MEMORY 37 / LLM-API 30 / DOC-ONLY 30 / TOOL 16 / FINANCE 9 / ECOMMERCE 5 / OFFICE 4 / SKILL-CN 4 / UNKNOWN 68 / SKIP 7。
被排除：searxng（README 空）、web-check（空）、layerfs/mana（无 README）、claudecodeui（无首行）、dotfiles/ghostty-config（个人配置）。

### 最终高价值参考
| 项目 | 定位 | 选取理由 |
|------|------|---------|
| **CL4R1T4S**（26 家系统提示词） | 顶级产品提示词 | 直接回答「更懂用户的 agent」 |
| **freellmapi / new-api / nexus-llm-router / NewAPI-Gateway / free-claude-code** | 网关竞品 | 签名目录自更新 / 渠道加权 / 80+ 路由策略+rationale 审计 / 一键 CLI 配置 / 模型搜索+分层路由 |
| **skills-best-practices / superpowers / SkillOpt / ClaudeSkills / SkillSpector / benjamin-plus-skill** | skills 工程 | frontmatter 触发词 / 渐进披露 / 验证门禁 / 负反馈 / 安全扫描 / 注入式>可发现式 |
| **claude-mem / Agent_Memory_Techniques / TencentDB-Agent-Memory / mem0 / okf-agent-memory** | 记忆沉淀 | 四层异步管线 / 仅成功任务抽技能 / SKILL.md 契约 / 三层渐进披露 / provenance |
| **ccg-workflow / prompt-optimizer / claude-code-tips / claude-code-best-practice / LibreChat / clawdeck / ccstatusline / UI-TARS** | 边用边学 + 透明交互 | 语义化进度 / 决策树 / 讲解层 / Part 渲染 / trace 瀑布 / statusline / 事件流 |
| **MoneyPrinterTurbo / ppt-agent-skills / seedance-2.0 / EcomAgent / OfficeCLI / dashi-ppt** | 内容生产线 | 中间产物契约 / 断点恢复 / 编导层控制 / 进度阶段语义 / 电商/PPT 落点 |
| **DeepTutor / AstrBot / openclaude / Claudable / get-shit-done / LibreChat Landing / Learnova** | 新手 onboarding | 有反馈向导 / 空状态=欢迎页 / 一句话定位 / 错误分级 / 自导航 |
| **9router（上游）/ new-api / EcomAgent / seedance** | 直接演进 | 上游同源 + 生产级能力补强 |

置信度：主项目与网关竞品、CL4R1T4S 为 DIRECT；参考目录分类为 INFERENCE（子代理按名称+README 首行）；新增扩展方向（PPT/电商）为「产品方向推定」。

---

## 2. 主项目现状总结

**架构特点**：分层清晰（app→api→lib/db repos→open-sse 引擎→skills），deny-by-default 鉴权，requestDetails 全链路可观测（客户端请求/上游翻译/原始响应/PXPIPE），技能体系双源（内置常量 + 用户 DB）。

**当前优点**：repo 模式、限流与探针、skillsRepo 原子 `json_set` 自增、真实 LLM E2E、dashboard 18 页齐全、技能工程化约束已落地大部分（P0/P1/T03）。

**主要问题（有证据）**：
1. **docs 漂移**：`9router-template`、`9router-development` 未被 SKILLS 常量/README/dashboard 索引 → 新手不可发现（F1 报告）。
2. **Console Log 页零教学**：纯裸日志、无字段解释/错误解释/跳转引导；挂在 `/api/translator` 命名空间（归属不清）（A1/F1）。
3. **skillsRepo 无负反馈**：`recordSkillUse` 只 `+0.1*(1-c)` 向 1 收敛，无衰减、无 downvote、无来源分级（A1）。
4. **POST 只防长度不防质量**：无 description 触发词校验、无内容结构门禁（B4）。
5. **chat 页死链**：`BasicChatPageClient.js:639` fetch `/api/dashboard/chat/completions` → **仓库无此路由（无 src/app/api/dashboard、git 全历史无、next.config 无重写）→ 实为 404 死链**（已三次交叉验证）。basic-chat 还被注释隐藏（Sidebar:23）。
6. **无 onboarding / 无透明度抽屉 / 首页=Endpoint 技术页**（B9/B8/F1）。
7. **video 数据一致性已修复**（skills.js 含 video；README 含 video；目录 11 个 SKILL.md vs 常量/README 9 项——多 template/development）。

**风险点**：付费 API 只 Mock 不真实调用（已知边界）；限流默认关；observability 默认开（ENABLE_REQUEST_LOGS 缺省时 enableObservability true）。

**技术债**：`9router-template`/`9router-development` 未索引（文档债）；`/api/dashboard/chat/completions` 死链（前端债）；skillsRepo confidence 无衰减（设计债）；Console Log 挂 translator（归属债）。

---

## 3. 参考项目亮点提炼

### CL4R1T4S（26 家 · 60+ 提示词）
- **厂商×维度矩阵**：人格语气（Muse 反套话/温暖克制）、行为守则（Claude 搜索决策树、Cursor lint≤3、Windsurf 并行、Manus todo）、透明化（Codex ✅⚠️❌徽章、Devin think、MultiOn EXPLANATION）、新手容错（Replit 面向非技术+3次回滚建议、Dia 15 词内先答、Cluely 90% 置信门槛）、记忆（ChatGPT bio 协议、Windsurf 持久记忆、MultiOn USER CONTEXT）。
- **15 条可迁移规则**（详见 sub-report）：先答后问单问题上限 / 开场 15 词 / 工具命名化讲人话 / 验证徽章 / DONE-BLOCK 协议 / 环境问题上报不硬修 / EXPLANATION 每步解释 / 进度 todo / 引文强制 / "我的猜测是…"降级 / NEVER 负面清单独立成节 / bio 记忆协议 / 搜索决策树 / 面向非技术语言 / 数据真实性。
- **3 个最贴合模式**：① Anthropic SKILL.md 体系（先读再动手、description 五要素）② Cluely 分场景响应协议（清晰/模糊/数学/UI 导航各一套话术）③ Devin planning/standard/edit 三态机。

### 网关竞品
- **签名实时模型目录**（freellmapi：Ed25519 验签拉取，自更新）→ 治「provider 接入仍偏手工」。
- **80+ 路由策略 + 全量 rationale 落库**（nexus-llm-router：`request_id+selected_model+strategy+rationale`）→ 治「黑匣子日志埋没」最直接。
- **`setup-<client>` 一键 CLI 配置**（freellmapi）→ 小白最友好形态。
- **Admin UI 模型下拉搜索 + 按 Fable/Opus/Sonnet/Haiku 分层路由**（free-claude-code）→ 治「不知道选哪个 provider」。
- **价值评分加权路由 + 5min 路由表重建**（NewAPI-Gateway）。
- 排除：free-api（captcha 破解）、FreeToken（本地推理）、gpt2api/claude-connect/cc-gateway（参考目录无此名，已核对）。

### skills 工程
- **frontmatter 触发词≥3 + 负面触发**（skills-best-practices：description 是 agent 触发前唯一可见字段）。
- **渐进披露**：SKILL.md<500 行 + references/scripts 一层深 + JiT。
- **路由评测**：3 正例/3 反例 + LLM 批判（skills-best-practices）、L1+L2 路由评测（ClaudeSkills）、TF-IDF 词法近似（agent-skills）。
- **验证门禁 held-out**（SkillOpt：只有严格优于验证分数才接受编辑）、`no_regression`（SkillOpt-Sleep）。
- **负反馈**：ClaudeSkills「三件套」（验收标准/不做什么/陷阱）、SkillSpector 71 漏洞模式安全门。
- **注入式>可发现式**（benjamin-plus-skill：注入 −17.9% vs 文件夹式 −0.5%）。
- **诚实边界/反模式/迭代上限**（nuwa-skill）。

### 记忆沉淀
- **四层异步管线**（TencentDB：L0 原始 → L1 事实/偏好 → L2 场景 → L3 Persona；批量+空闲+倍增触发 `everyN=1→2→4→8`）。
- **仅成功任务抽技能**（Agent_Memory_Techniques/11：procedural 只从成功执行抽）。
- **SKILL.md frontmatter 契约**（TencentDB skill-format：name/description/category/source/resources，body≤50KB）。
- **三层渐进披露召回**（claude-mem：索引→时间线→详情，省 ~10x token）。
- **provenance 贯穿**（graphiti 事实可回溯 episodes；claude-mem `<private>` 标签隐私）。
- **零 LLM 也可用**（okf：BM25+frontmatter，300µs）。

### 边用边学 / 透明交互
- **语义化进度行**（ccg-workflow `parser.go`：`[PROGRESS] session/turn/message/cmd_done`）。
- **决策树式入口选择**（ccg-workflow workflows.md 5 分支 ASCII 决策树）。
- **「左右心智模型」文档**（prompt-optimizer：左侧在改什么/右侧在看什么）。
- **Part 化消息渲染**（LibreChat：Reasoning/Thinking/ToolCall/SubagentCall 独立可折叠；continue StepContainer；claudecodeui 工具绑定渲染）。
- **statusline widget 化 + 可配置**（ccstatusline 40+ widget）。
- **trace 瀑布 + UNKNOWN 三态 + What? 证据链**（clawdeck）。
- **最短可跑路径优先**（prompt-optimizer quick-start 4 步；claude-code-tips 光谱定位）。

### 内容生产线（媒体/PPT/电商）
- **中间产物契约 + 断点恢复**（ppt-agent-skills `planningN.json→slide-N.html→slide-N.png` 每页 Gate；MoneyPrinterTurbo 批次 stop-at）。
- **编导层控制**（seedance-2.0 首帧回读再续拍；talkcraft SHOTBOOK 语义锁）。
- **进度阶段语义**（Pixelle「分镜 3/5 - 生成插图」；VideoHub SSE 阶段推送）。
- **产出即可编辑资产**（FaClip 字幕即接口、dashi-ppt 浏览器编辑器、hyperframes HTML 即中间媒）。
- **幂等键 + 四层 HITL**（EcomAgent：RBAC→业务规则→模式门→Human-in-the-Loop；`permission_request` 挂起整个 turn）。

### 新手 onboarding
- **有反馈的向导**（DeepTutor 现场连通性自测；AstrBot 步骤状态卡；GSD 交互式安装器）。
- **空状态=欢迎页**（LibreChat Landing 自适应问候 + `{{user.name}}`；Law of 3：一个 CTA/一段说明/一个资源链接）。
- **一句话白话定位 + 决策段**（openclaude non-technical-setup「你不需要 Bun」；Which Provider 决策段）。
- **错误分级 + 防呆门**（EcomAgent 写操作挂起人工批准；ppt-agent-skills 机器 QA 代替人眼）。
- **自导航 + 渐进披露**（GSD `/gsd-progress`/`/gsd-next`；Learnova 配置缺失确定性本地兜底）。

### 共性工程优点（跨桶）
渐进披露、验证门禁、诚实边界、负反馈、provenance、语义化进度、空状态=欢迎页、一句话定位、幂等、错误分级、机器 QA、注入式>可发现式。

---

## 4. 差距分析

| 层 | 主项目现状 | 参考最佳实践 | 差距 |
|----|-----------|-------------|------|
| **功能** | 单次生成（image/video/tts/stt 各自端点） | 生产线（脚本→素材→成片/PPT/Listing） | 无跨能力编排、无中间产物契约 |
| **功能** | `/v1/videos` async job（request_id→poll→下载） | seedance 续拍/VideoHub 断点/幂等键 | 无 stage 语义、无恢复点、无 SSE 推送 |
| **架构** | 组合回退=顺序 fallback | nexus 80+ 策略 + rationale 落库 | 无「为什么这样路由」审计 |
| **工程** | 技能双源已好 | frontmatter 触发词/路由评测/负反馈/安全扫描 | 无触发词校验、无负反馈、无质量门禁 |
| **工程** | docs 漂移（template/development 未索引） | — | 新手不可发现 |
| **工程** | `/api/dashboard/chat/completions` 死链 | — | chat 页实际 404 |
| **性能** | rateLimit 单点令牌桶 | freellmapi (平台,模型,密钥) 粒度 | 粒度偏粗（待验证） |
| **安全** | deny-by-default 好；console-log 挂 translator | — | 归属不清（低危） |
| **运维** | requestDetails 已全链路 | — | 缺 rationale/缺 message 级关联 |
| **测试** | 21+13+5 单测绿；e2e 冒烟有 | skills-best-practices 路由评测/行为 eval | 无技能路由评测 |
| **用户体验** | 首页=Endpoint 技术页；无 onboarding | AstrBot/LibreChat/openclaude | 小白首次成功路径缺失 |

---

## 5. 可迁移 / 半可迁移 / 不建议

### 可迁移（直接做，低风险）
| 项 | 原因/证据 | 收益 | 成本 | 风险 | 适配 |
|----|----------|------|------|------|------|
| **技能索引三源对齐**（SKILLS 常量/README/dashboard 收录 template+development） | 3 文件漂移 | 新手可发现 | 5min | 无 | skills.js + README + page 文案 |
| **skillsRepo 负反馈**（`POST /api/skills/[id]/reject`，confidence 反向收敛 + stale 标记） | SkillOpt held-out / ClaudeSkills 三件套 | 失败技能浮出 | 2h | 低（kv 兼容） | 新增 repo 方法 + 路由 + 前端 downvote |
| **POST 质量门禁**（description 触发词≥1 + content 含≥2 `##` 结构） | skills-best-practices P1/P3/P4 | 防误触发/防垃圾 | 2h | 中（需兼容存量空 description） | 校验层 add-on，存量打「待完善」标 |
| **Console Log 页教学化**（字段解释、错误解释、跳转、空状态引导） | B8/B7 语义化日志/错误解释 | 黑匣子打开 | 4h | 低 | 纯前端 |
| **chat 死链修复**：路由文件补上（代理到 /v1/chat/completions 或独立薄层）或隐藏入口（Sidebar 注释回滚） | 全仓库无定义 | 页面可点/不可误导 | 2h | 低 | 新增路由 + Sidebar 显示 |
| **chat 页透明度抽屉**（消息级「过程」折叠：耗时/用量/路由规则命中） | LibreChat Part / clawdeck trace | 边用边学 | 8h | 中 | BasicChatPageClient 增加折叠区 |
| **空状态三格新手卡 + placeholder 动态三态** | B9 三卡/placeholder 三态 | 首次成功路径 | 3h | 低 | basic-chat |
| **dashboard onboarding**（首次 localStorage `hasSeenWelcome` 3 步微引导） | AstrBot / GSD | 小白引导 | 4h | 低 | 首页横幅 |
| **请求 rationale 落库**（chat 响应附加 `route_reason`，dashboard 展示） | nexus-llm-router / ccg | 黑匣子 + 教学 | 8h | 中 | open-sse 注入 + usage 页展示 |

### 半可迁移（需 UI 编排/更多依赖）
- Devin planning/standard/edit 三态机（chat 页模式切换）。
- 签名目录自更新（freellmapi：需加验签通道与远端服务）。
- video 生产线接口（batch/sequence 变体 + SSE + 幂等键）。
- 电商×技能 / PPT×技能（先出 skills 模板/合同 JSON）。

### 不建议迁移
- Codex citations 语法、Gemini immersive 标签、Grok X 全家桶、MultiOn 手写协议（平台绑定太深）。
- LightRAG/graphiti 重型图谱（超出 SQLite 阶段）。
- SkillSpector 全量 71 模式（先做基础静态扫描）。
- 电商发布动作默认 EXECUTE（应 ASK，对齐 EcomAgent HITL）。

---

## 6. 优先级优化路线图

### P0（不改就烧钱/用不起来）
| 项 | 做什么 | 为什么 | 模块 | 依赖 | 收益 | 风险 | 迁移 | 兼容 | 验证 | 回滚 |
|----|--------|--------|------|------|------|------|------|------|------|------|
| **P0-A** 技能三源对齐（索引 template/development） | skills.js + README + page | 新手不可发现=用不起来 | 3 文件 | 无 | 高 | 无 | 否 | 是 | 单测+页面 200 | 回滚 commit |
| **P0-B** `/api/dashboard/chat/completions` 死链修复 | 新增薄代理路由（复用 handleChat）或隐藏入口 | chat 页 404 | 1 路由 | 无 | 高 | 低 | 否 | 是 | curl 200 | 删除路由 |
| **P0-C** Console Log 页教学化 | 字段解释+错误解释+跳转+空状态 | 黑匣子 | 1 页 | 无 | 中高 | 低 | 否 | 是 | 单测+页面 | 回滚 |

### P1（高价值改造）
| 项 | 做什么 | 为什么 | 收益 | 风险 |
|----|--------|--------|------|------|
| **P1-A** skillsRepo 负反馈（reject + stale） | 让失败技能浮出 | 高 | 低 |
| **P1-B** POST 质量门禁（触发词+结构） | 防误触发/防垃圾 | 中 | 中 |
| **P1-C** chat 透明度抽屉 + 空状态三卡 | 边用边学 | 中高 | 中 |
| **P1-D** 请求 rationale 落库 + 展示 | 黑匣子+教学 | 中 | 中 |
| **P1-E** dashboard onboarding（3 步微引导） | 小白首次路径 | 中 | 低 |
| **P1-F** 技能路由评测（3 正/3 反样例） | 防误路由 | 中 | 中 |

### P2（锦上添花）
- 签名目录自更新（需远端服务）、video 生产线接口（SSE+幂等）、电商×技能模板、PPT×技能模板、技能陈旧标记+Core/On-demand 分级、技能安全静态扫描。

---

## 7. 全栈实施方案

- **前端**：skills 页索引补 template/development + 提示；Console Log 页加教学头（字段解释表/错误解释/跳转 Skills）；basic-chat 空状态三卡 + placeholder 三态 + 透明度抽屉；首页 onboarding 横幅 + 状态卡；usage 页 rationale 列。
- **后端**：新增薄代理路由 `/api/dashboard/chat/completions`；skillsRepo 加 `rejectSkillUse`（confidence 反向收敛）；POST 质量门禁（触发词≥1、结构≥2 `##`）；chat 响应附加 `route_reason`（从 chatCore 注入，fail-open）。
- **数据层**：无迁移。负反馈/门禁走既有 kv + 校验层；rationale 走 requestDetails.data（无 schema 变更）。如需 message 级关联 → 追加 requestDetails.data 内嵌字段（不新建表）。
- **接口层**：`/api/skills` 增 `POST /[id]/reject`（400/404/500 语义与现有一致）；`/api/dashboard/chat/completions` 新路由；`/api/usage/request-details` 返回含 route_reason（后端已存，前端展示）。
- **配置**：无新 env（观察性沿用 ENABLE_REQUEST_LOGS/observability）。
- **日志与监控**：Console Log 教学化；rationale 进请求详情；无新监控设施。
- **测试策略**：skillsRepo 负反馈/质量门禁单测（RED→GREEN）；死链路由 E2E（curl → 200 流式）；透明化与 onboarding 前端静态 + 单测 +（需真机核验标注）；回归用 verify-no-regression.mjs。
- **部署与运维**：随 v0.5.76 发布；L1/L2 分批复用现有 CI。

---

## 8. 实施授权状态

**等待用户批准批次**。建议优先批准：P0-A（5min）、P0-B（2h）、P0-C（4h）为第一批（低风险、纯前端+1 薄路由）；P1-A/P1-B 第二批（repo+API）；P1-C/D/E/F 第三批。每批独立 commit、可回滚。需单独审批：数据库结构变更（当前无）、安全敏感改动（无）、生产部署（发布时确认）。

---

## 9. 需要用户补充的信息

1. **是否批准进入 Phase B？** 若批准，请给出批准批次范围（P0 全批 / P0+P1 / 逐项）。
2. **basic-chat 页是否应该重新显示在导航？** 当前被注释隐藏（Sidebar.js:23），但它是最贴近「小白用 AI」的页面。建议：修复死链并显示（默认关闭，可从首页 onboarding 引导进入）。若你倾向隐藏，仅做死链修复不显示。
3. **是否需要我把「P1-D 请求 rationale」也纳入第一批？** 它涉及 open-sse 引擎层（`chatCore` 注入），风险中，可独立批次。
4. **关于观察性默认开启**：当前 `ENABLE_REQUEST_LOGS` 缺省时 observability 默认 true（写 requestDetails）。若你希望默认关（省存储），告诉我；默认我按「保持现状」处理。
5. **电商/PPT/视频生产线扩展**：属于 P2 新产品方向，需要你明确是否作为下一阶段主线；建议先从「技能模板+合同 JSON」的 skills 层做起（无新基础设施）。
6. 其他需要对齐的：**`9router-template`/`9router-development` 是否也要出现在 dashboard 内置技能列表**（当前只收能力技能）。我倾向把它们索引为「开发者技能」子区，不污染能力列表——你可以决定。

---

## Evidence Ledger（摘要）
| Claim | Evidence | Type |
|-------|----------|------|
| 主项目 v0.5.75 工作树源码零改动 | `git status`（src/ open-sse/ skills/ tests/ cli/ docs/ 全空） | DIRECT |
| skills.js 9 项含 video；目录 11 项；README 9 项 | 三源清单核对 | DIRECT |
| template/development 未索引 | rg 三处无命中 | DIRECT |
| Console Log 零教学 | ConsoleLogClient.js 全量读 | DIRECT |
| `/api/dashboard/chat/completions` 死链 | find/rg/git 全历史三次交叉无定义 | DIRECT |
| skillsRepo confidence 无衰减 | skillsRepo.js:140 | DIRECT |
| POST 无质量门禁 | /api/skills/route.js | DIRECT |
| requestDetails 全链路已存在 | requestDetailsRepo.js + RequestDetailsTab.js | DIRECT |
| CL4R1T4S 26 家 15 规则 3 模式 | C1 逐文件通读 | DIRECT |
| 868 项目分类 | T04 按名称+README 首行 | INFERENCE |
| 参考目录=上游分支 | 目录结构高度相似 | INFERENCE |

## Next Gate
等待用户确认批次（上节第 1 项）→ 进入 Phase B（B1 影响分析 → B2 批次输出 → Builder 实施 → 定向验证 → Critic 独立审查 → Evaluator 终验 → 更新 workflow_status.md）。