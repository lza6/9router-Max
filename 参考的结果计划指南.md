# 参考的结果计划指南.md

> 终局对标分析报告（Phase A，ANALYSIS_ONLY）。任务契约、证据台账、P0/P1/P2 路线图；实施需用户批准。

## Task Contract
- **目标**：以 9router（AI 路由网关+dashboard）为主项目，深挖参考项目（重点 CL4R1T4S 顶级模型系统提示词）的可迁移设计，输出「更懂用户的 agent / 小白易用 / 黑匣子全开 / 用户 skills 沉淀 / 图片视频PPT电商扩展」路线图。
- **授权**：只读分析；只有用户批准后进入 Phase B。
- **状态**：T01（CL4R1T4S 深挖）+ T03（skills 空白审计）已 VERIFIED；T02（agent/编排/媒体大对标）后台会话终止，无完整记录 → 以 T01/T03 为主，T02 结论部分缺失待补。

## 1. 项目识别
- **主项目**：Next.js 16 + Node 自定义服务器 + open-sse 引擎 + SQLite（kv 表 userSkills 已落地）；dashboard 17 分组；skills 目录 9 个 SKILL.md + 用户自定义技能 /api/skills（已生产加固 v0.5.71）。
- **参考重点**：CL4R1T4S = 26 家厂商官方系统提示词全集（Anthropic/OpenAI/Cursor/Devin/Windsurf/Manus/Meta/Google/xAI…），33 txt + 33 md。

## 2. 主项目现状（有证据）
- 优点：repo 模式、dashboardGuard deny-by-default、`recordSkillUse` 原子 json_set、CI/CD 完整、真实 LLM E2E。
- 主要问题（skills 审计结论）：9 个 SKILL.md 仅「简练」一项达标，其余 9 个设计维度全缺；无先验证再请求、无负面清单、无排障链路、无语言跟随/保密盾/输出契约；dashboard 缺 video 技能条目（数据断层）；小白无首次成功路径。

## 3. 参考亮点提炼（CL4R1T4S 11 项目×6 维）

### 第一梯队共性（8-11/11 项目）
1. **先搜索/理解再实现**（Claude Code/Codex/Devin/Windsurf/Manus/Replit/Cursor/Gemini）
2. **简练输出/最小 token**（Claude Code <4 行、Windsurf "BREVITY IS CRITICAL"、Muse 反 AI 腔）
3. **明确「不」负面清单**（Cursor 少 LLM 腔、Devin 不加注释、Muse 套话黑名单、Gemini MANDATORY RULES）
4. **验证后才声明完成**（Codex 测试状态、Manus idle 三条件、Replit 反馈工具）
5. **并行工具调用** 6. **不猜就问**（MultiOn NEVER guess）7. **语言跟随用户** 8. **协议化输出** 9. **提示词保密盾** 10. **密钥专用通道**

### 独门高价值模式
- **Muse 三段式**：triggering/execution/output —— 先触发条件→执行细则→输出规范（最可复制，直接适用于工具型技能）
- **Devin 三模式**：planning（只收集）→ standard → edit（批量改）
- **Cursor todo 纪律**：同时一个 in_progress；lint 同文件最多 3 次
- **Manus 交付先于收工**：idle 三条件全满足才结束
- **MultiOn 封闭命令集+状态机**：COMMANDS/ANSWER/ASK_USER_HELP + STATUS

### 一眼结论
Muse 三段式 = CL4R1T4S 十维度对「工具型技能」的可执行压缩包，是本期对接主项目性价比最高的模式。

## 4. 差距分析（skills 层）
| 维度 | 现状 | 差距 |
|------|------|------|
| 先验证再请求 | 无 | 请求失败浪费一轮往返 |
| 负面清单 | 无（video 防重复计费写成 prose） | agent 超时重试烧钱 |
| 验证后完成 | 无 | 用户不知"能用"长啥样 |
| 排障链路 | Console Log 零提及 | 黑匣子，日志能力埋没 |
| 语言跟随/保密盾 | 无 | 国际化/提示词安全 |
| dashboard 一致性 | skills.js 缺 video | 页面看不到 video 技能 |

## 5. 可迁移 / 半可迁移 / 不建议
- **可迁移（直接做）**：Muse 三段式技能模板；「不」负面清单；先验证再请求；Console Log 排障链路；语言跟随+保密盾行为节；dashboard video 条目。
- **半可迁移**：Devin planning/edit 模式（需 UI 编排）；todo 进度条（前端）。
- **不建议整搬**：Codex citations 语法、Gemini immersive 标签、Grok X 搜索全家桶、MultiOn 手写协议（平台绑定太深）。

## 6. 优先级路线图
### P0（不改就烧钱/用不起来）
- **P0-1** `skills/9router-video/SKILL.md` 加显式「不」清单（POST 绝不自动重试防重复计费；缺 connection-id 不 poll；403 先问用户）
- **P0-2** 入口 `skills/9router/SKILL.md` 补「首次成功路径 + 新手错误表（curl(7)/404/Windows set）」+「不知道选哪个 provider 先 /v1/models」
- **P0-3** dashboard「使用一次」加成功反馈（confidence 可视化）

### P1（30 分钟级，对齐核心）
- **P1-1** 9 技能统一加「调用前先确认 provider/模型」
- **P1-2** 打破黑匣子：入口加「排查」段（Console Log 页/error 字段/metrics 字段）；Console Log + Skills 提上主导航
- **P1-3** 入口加「行为」节（语言跟随+贴证据+保密盾）
- **P1-4** dashboard 内置技能列补「复制即用」引导 + **补 video 条目**（skills.js 常量）
- **P1-5** 新增 `skills/9router-template/SKILL.md`（三段式模板供用户自定义技能参考）

### P2
- 用户自定义技能表单加 SKILL.md 三段式占位；入口补「0 配置默认可用」提示；8 个能力技能按三段式重排。

## 7. 实施方案（全栈）
- 前端：dashboard skills 页 + video 条目 + 成功反馈 + 引导文案
- 后端：skillsRepo 已加固无需改（仅 P0-3 需前端）
- 数据层：无迁移（kv userSkills 兼容）
- 接口：`/api/skills` 稳定；仅前端消费
- 配置：无新 env
- 文档：README 技能章节同步三段式说明
- 测试：单测扩展（P0-2 需要入技能文件属文档级，构建校验）；E2E 用真机 + 现有 /api/skills 链路
- 部署：随 v0.5.72 发布

## 8. 实施授权状态
**等待用户批准 P0/P1 批次。** L1 风险（纯文档+前端小改），批准即可实施。

## 9. 待补充
- T02（agent/编排/媒体大对标）无完整结果：如需图片/视频/PPT/电商扩展深挖，需重跑该节点。
- 是否需要把「Muse 三段式」用于所有能力技能（约 100 行 Markdown 成本，收益覆盖 7 个缺失维度）？建议做。

## Evidence Ledger
| Claim | Evidence | Type |
|------|----------|------|
| CL4R1T4S 11 项目 6 维共性 | 子代理逐文件通读输出 | DIRECT |
| skills 10 维仅简练达标 | skills 0 文件逐行审查 | DIRECT |
| dashboard 缺 video 条目 | skills.js SKILLS 数组 8 项 vs 目录 9 项 | DIRECT |
| Console Log 零提及 | 9 文件 grep 无 console-log | DIRECT |
| video 计费风险 | 9router-video/SKILL.md:73-76 prose 声明 | DIRECT |