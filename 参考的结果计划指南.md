# 参考的结果计划指南.md

> 本文件是"9router 主项目 + 848 个参考项目对标分析"任务的单一状态源与最终报告。
> 阶段：Phase A 已完成 → Phase B 实施完成（技能沉淀体系批次已落地闭环）。

## Task Contract

- **原始目标**：以 9router（AI 路由网关 + dashboard）为主项目，扫描 `参考项目` 下 848 个项目，识别高价值参考项，提炼设计思想/工程规范/交互流程，完成差距分析与跨技术栈适配，输出分优先级路线图，并**真实 E2E 测验、验收、审计后提交推送、创建发行版**。
- **当前阶段**：Phase B 完成（批次 1：Skills 动态化）
- **当前授权**：用户明确要求"完整落地闭环 + 真实 E2E + 提交推送 + 创建发行版"，已执行。
- **成功标准**：9 节报告交付 + 至少一个高价值批次真实落地（代码+测试+运行证据）+ 提交推送。✅
- **停止条件**：批次验收通过、无未解决 P0/P1、报告与代码一致。✅

---

# 一、项目识别结果

## 1.1 主项目识别（9router-Max v0.5.69）

| 维度 | 结论 | 证据 |
|------|------|------|
| 类型 | 本地 AI 路由网关 + Web dashboard（单体，Next.js 全栈） | package.json / docs/ARCHITECTURE.md |
| 技术栈 | Next.js 16 + React 19 + Node 自定义 HTTP server + 纯 JS(ESM) | package.json / next.config.mjs / custom-server.js |
| 包管理 | npm（+bun 变体） | package.json scripts |
| 构建 | `next build --webpack` → standalone 产物 | next.config.mjs `output:"standalone"` |
| 运行入口 | `custom-server.js`（HTTP 包装：IP 还原、h2c 降级、token 注入）→ `server.js` | custom-server.js |
| 路由引擎 | `open-sse/`（provider 无关）：29 executors、123 provider registry、translator 注册制 | open-sse/executors、providers/registry、translator |
| 持久化 | SQLite 适配链 bun:sqlite→better-sqlite3→node:sqlite→sql.js；11 个 repos | src/lib/db/ |
| v1 API | chat/embeddings/images/audio/videos/search/web.fetch 全兼容面 | src/app/api/v1/ |
| 前端 | dashboard 17 分组（providers/combos/cli-tools/media-providers/console-log/skills/…） | src/app/(dashboard)/ |
| 部署 | Dockerfile + docker-compose + cli/ 启动器（npm 包 9router） | Dockerfile / cli/ |
| 测试 | vitest 独立包 `tests/`（~938 pass，~64 已知 fail） | tests/ + __baseline__ |

## 1.2 参考项目扫描结果（848 个目录）

- 技术栈分布：node ~260、python ~210、go ~45、rust ~35、知识库/文档 ~300
- 来源确认：参考目录中的 `9router/` 是上游原版（v0.5.65, github.com/decolua/9router），主项目是它 + 自定义扩展（v0.5.69，remote: github.com/lza6/9router-Max）；两版 src 仅 10 个文件级差异，主项目为超集（新增 opencode-go.js executor 等）。

## 1.3 候选参考项目清单与评分（0-5）

### 高价值参考（精扫，评分 ≥4）
| 项目 | 技术栈 | 定位 | 业务相似度 | 架构参考 | 工程成熟度 | 可迁移性 | 评分 |
|------|--------|------|:---:|:---:|:---:|:---:|:---:|
| **NewAPI-Gateway** | Go | 多供应商透明聚合网关 | 5 | 5 | 4.5 | 4 | **4.6** |
| **EcomAgent** | Python | 电商 agent runtime（任务编排/Tool 安全/技能沉淀） | 3.5 | 5 | 4.5 | 4 | **4.3** |
| **agentmemory-main** | TS | Agent 持久化记忆（lessons 强化/分级） | 3 | 4.5 | 4 | 4 | **3.9** |
| **LibreChat** | TS | AI 对话平台（Agent Builder/Marketplace/插件） | 4 | 4.5 | 4.5 | 3 | **4.0** |
| **Claude-Code-Agent-Monitor** | TS | Agent 会话可视化看板（Kanban/观测） | 3 | 4 | 4 | 4 | **3.8** |
| **auth2api** | TS | 单账号健康中转（cooldown/重试/隐身） | 4.5 | 4 | 3.5 | 4.5 | **4.1** |
| **aisix** | Rust | AI 网关（治理/guardrails/PII/缓存） | 4 | 4 | 4.5 | 2.5 | **3.8** |
| **Infographic** | TS | 声明式信息图/PPT 生成引擎（AI 友好） | 2 | 4 | 4 | 3 | **3.3** |

### 局部参考
Sparrowgate（LLM 缓存）、OpenHands Agent Canvas（ACP 多后端）、graphiti（时态记忆图）、MoneyPrinterTurbo/FunClip（视频管线）、OpenSpec（spec 驱动）、Stratum（DDD 多租户）、Casbin-gateway（细粒度授权）、code-review-graph（变更影响分析）。

### 排除/低价值
- 纯文档/awesome-list 类（~300 个）：作为灵感库保留，不作为迁移对象
- 与主项目无关领域的独立工具（视频编辑、CV、量化、办公等）：仅作为"横向扩展面的案例参考"
- 恶意/可疑工具（AutoSploit 等自动化利用类）：**不参考不迁移**

---

# 二、主项目现状总结

## 2.1 架构特点
- 分层清晰：`src/app/api/*`（API 面）→ `src/sse/`（app 侧 glue：鉴权/账号选择/媒体调度）→ `open-sse/`（provider 无关引擎）→ `src/lib/db`（SQLite repos）
- 边界意识好：`open-sse/AGENTS.md` 明确定义"如何在引擎加 provider/executor/translator"
- 已具备横向扩展骨架：v1 已有多模态端点 + mediaConfig SSRF 防护 + 媒体 provider 目录

## 2.2 当前优点（有证据）
1. **账号健康状态机成熟**：模型级锁（`modelLock_${model}`）+ 指数退避 + 精确 resetAt（`src/sse/services/auth.js:239`）
2. **错误分类规则表完善**：text+status 双维匹配（`open-sse/config/errorConfig.js` ERROR_RULES）
3. **token 刷新链路完整**：backgroundTokenRefresh + checkAndRefreshToken
4. **透明代理安全**：custom-server 只信 loopback 代理的 XFF，peer token 防伪
5. **供应商生态丰富**：123 registry 文件、29 executors、translator 直接路由

## 2.3 主要问题（已确认/有风险）
1. **账号选择缺"价值评分"**：fill-first/round-robin 是确定性顺序，不感知成本/余额/近期用量/健康成功率 → 无法自动把流量导向"最划算且健康"的账号（对比 NewAPI `computeRouteValueScore`）
2. **流式响应无"可行动输出"检测**：非流式有 `Invalid SSE response` 兜底，但流式转发不判断 `meaningful delta`，上游返回空/错误包络时会透传给客户端而非触发 fallback（对比 NewAPI `classifySSEDataLine`+无效响应抑制）
3. **Skills 是静态清单**：`src/shared/constants/skills.js` 硬编码 GitHub URL 列表，用户无法保存自己的技能、无使用统计、无强化（对比 EcomAgent `save_skill` + agentmemory `reinforceLesson`）
4. **无响应有效性可观测面板**：console-log 是原始行日志，无结构化事件/成功率/时延看板
5. **自动化签到/健康巡检缺失**（NewAPI 有 checkin+cron+backup_webdav）

## 2.4 风险点与技术债
- `docs/ARCHITECTURE.md` 持久化章节已过时（db.json → SQLite），CLAUDE.md 已注明
- `cloud/` worker 目录不在仓库，`embeddings.cloud.test.js` 必失败
- 主项目无跨版本 CI 回归门禁脚本一键入口（需手动 `verify-no-regression.mjs`）

---

# 三、参考项目亮点提炼

## 3.1 按项目

### NewAPI-Gateway（网关层最大对标）
- **价值评分路由** `computeRouteValueScore`：`costScore=1/(1+unitCost)` × `budgetScore=(balance+1)/(balance+recentUsage+1)` → 便宜+余额充足+近期用得少的账号自动优先（model_route.go:653）
- **健康乘数** `finalizeRouteHealthStat`：成功率×0.75+时延分×0.25，失败惩罚 `exp(-α·failRate)`，奖励 `1+β·healthScore·confidence`，clip 到 [0.05, 1.12]（model_route.go:1058）
- **加权随机洗牌** `weightedShuffleAttempts`：同优先级内按 Contribution 加权随机，避免固定顺序被风控识别
- **优先级分层重试**：先试完同一 priority 内所有路由再降级（controller/relay.go:66）
- **无效响应抑制**：流式逐行 `classifySSEDataLine`（ParseFailed/ErrorEnvelope/MeaningfulDelta），抑制窗口+冷却 15min
- **透明代理保真**：只转发安全头、删 X-Forwarded-*/Via，`rewriteRequestModel` 改 model 名
- **自动签到 + cron + WebDAV 备份**：运营级自洽

### EcomAgent（Agent Runtime 层）
- **Template Method 编排**：技能解析→前置→委托→工具循环→完成（base_agent.py）
- **PreToolUse 权限管道**：只读前缀判定 + ASK 阻塞确认 + AbortHandler
- **save_skill 元技能**：模型经 HITL 确认后热写入 SKILL.md（技能沉淀闭环）
- **SKILL.md 渐进披露**：菜单常驻、load_skill 才注入正文 → 控制上下文
- **上下文压缩策略**：`context_window - 13k 安全边际` 动态阈值，结构化摘要折叠（context_policy.py）

### agentmemory（记忆强化层）
- **lessons 强化**：`reinforceLesson` — 每复用 +1，`confidence += 0.1*(1-confidence)` 收敛到 1 → 用得多=越信任（lessons.ts:7）
- **指纹去重** `fingerprintId`：内容哈希 → 同一 lesson 只存一份

### LibreChat（平台层）
- **Agent Builder**：Skills/MCP/Code Interpreter/工具编排一个市场统一配置
- **Agent Marketplace**：社区技能可发现可部署
- **Agent Plugins**：部署 Skills+MCP+命令钩子打包成启动插件

### Claude-Code-Agent-Monitor（观测层）
- **会话看板**：Working/Waiting/Completed/Error 四态 Kanban，Waiting 悬浮显示原因
- **Hook 集成**：走 Claude Code 原生 hook 采集会话/工具/子代理
- **自带 MCP**：看板操作可被 agent 调用

### auth2api（中转层）
- **账号 cooldown/refreshPromise**：`refreshing` 单飞锁防并发刷新；cooldown 到期找"最可恢复账号"
- **cloaking 指纹伪装**：复刻客户端指纹算法，上游不可感知代理存在（谨慎参考——合规风险，不迁移）

### aisix（治理层）
- **API key 只存哈希**：明文不落库
- **guardrails**：keyword/regex + PII 检测 + 前置/后置内容策略
- **限流多维**：RPS/RPM/RPH/RPD + TPM/TPD + 并发上限

## 3.2 共性优点
1. **路由/账号选择都是"数据驱动打分"**而非固定顺序
2. **失败闭环**：失败→分类→抑制/降权→冷却→恢复，全部自动化
3. **技能=一等公民**：可保存、可强化、可市场分发、渐进披露控制 token
4. **黑匣子可视化**：会话级看板、结构化事件、可被 agent 调用
5. **运营自洽**：签到/cron/备份/通知开箱即用

---

# 四、差距分析

| 层 | 主项目现状 | 参考实践 | 差距等级 |
|----|-----------|---------|:---:|
| 功能 | 账号选择 fill-first/round-robin | NewAPI 价值评分+健康乘数+加权洗牌 | **P1** |
| 功能 | 流式透传无有效性检测 | NewAPI 流式分类+无效响应抑制 | **P1** |
| 功能 | Skills 静态 URL 清单 | EcomAgent save_skill / agentmemory 强化 / LibreChat 市场 | **P1（本批实施）** |
| 功能 | 无自动签到/健康巡检 | NewAPI checkin/cron | P2 |
| 架构 | 单进程单库 | — | 无差距（本地形态合理） |
| 工程 | 测试基线混有已知失败 | NewAPI 全绿 | P2 |
| 性能 | 无 LLM 响应缓存 | Sparrowgate SQLite 缓存/便宜模型路由 | P2（需谨慎） |
| 安全 | 密钥加密存储/SSRF 防护已具备 | aisix API key 哈希 | P2 |
| 运维 | Docker+cli 已有 | NewAPI WebDAV 备份 | P2 |
| 观测 | console-log 原始行 | Claude-Code-Agent-Monitor 会话看板 | P2 |

---

# 五、可迁移 / 半可迁移 / 不建议迁移

| 项 | 分类 | 原因 | 收益 | 成本 | 风险 | 适配方式 |
|----|------|------|------|------|------|---------|
| 技能保存+强化 | **可迁移** | EcomAgent/agentmemory 设计独立于语言 | 高（用户点名） | 中 | 低 | JS 版 skillRepo + /api/skills + 前端管理页 |
| 账号价值评分 | 可迁移（半） | 需把余额/成本/用量接进选号 | 高 | 中高 | 中 | 在 getProviderCredentials 加评分排序，默认开关关闭 |
| 流式无效响应抑制 | 可迁移（半） | open-sse 核心，需逐行解析 SSE | 高 | 中 | 中 | chatCore streamingHandler 加分类器+冷却 |
| API key 哈希存储 | 可迁移（半） | 已有 API_KEY_SECRET 加密 | 中 | 低 | 中 | 兼容迁移，读旧密钥优先 |
| 自动签到 | 半可迁移 | 需各 provider 签到端点映射 | 中 | 中高 | 低 | 独立 service + cron |
| 会话看板 | 半可迁移 | 主项目无 hook 采集面 | 中 | 中高 | 低 | 用 usage 日志聚合生成 |
| 隐身指纹伪装 | **不建议** | 合规风险，绕过上游检测 | — | — | 高 | 不迁移 |
| LLM 响应缓存 | 暂不建议 | 流式+动态定价易缓存错乱 | 低 | 中 | 高 | 待用户决策 |
| guardrails PII | 半可迁移 | 已有 sanitizeSecrets | 中 | 低 | 低 | 增强脱敏点 |

---

# 六、优先级优化路线图

### P0（安全/稳定性/核心路径）
无未解决 P0。主项目安全基线（密钥加密、SSRF 防护、IP 防伪、SQLite 回滚链）已健全。

### P1（高价值改造）
1. **Skills 动态化**（本批）：保存/管理/强化用户技能 → 模块 skills 页+API+数据层 → 低风险，可 E2E
2. **账号价值评分**：可选开关的余额/成本/用量感知选号 → 中风险，需 mock 测试
3. **流式无效响应抑制**：meaningful-delta 检测+冷却 → 中风险，需核心改造

### P2（增强）
自动签到、WebDAV 备份、会话观测看板、API key 哈希迁移、文档同步（ARCHITECTURE.md 更新）。

---

# 七、全栈实施方案（针对 P1-1 本批）

### 前端
- `src/app/(dashboard)/dashboard/skills/` 升级：内置技能 + 自定义技能双 tab，增删改、热度排序、使用统计展示
### 后端
- 新增 `src/lib/db/repos/skillsRepo.js`：自定义技能 CRUD + 使用次数强化
- 新增 `src/app/api/skills/` 路由：GET 列表 / POST 创建 / PUT 更新 / DELETE / POST [id]/use
### 数据层
- 复用 kvStore/新表：无迁移（纯新增，不破坏现有 DB）
### 接口
- 兼容：原 `/dashboard/skills` 静态列表保留为"内置技能"来源，新 API 只叠加
### 配置
- 无新 env
### 测试
- `tests/unit/skills.test.js`：CRUD+强化逻辑
- E2E：真实启动服务 → curl 全链路 → 页面可访问性
### 部署
- 随标准 `npm run build`；提交+推送+tag+release

# 八、实施授权状态
用户指令"完整落地闭环 + 真实 E2E 测验验收审计 + 提交推送创建发行版"= Phase B 授权，且已执行完毕。

# 九、Phase B 实施结果汇总（v0.5.70，已提交并发布）
| 项 | 结果 |
|----|------|
| 代码落地 | `skillsRepo.js`（CRUD+uses+confidence 强化）+ `/api/skills` 三路由 + 仪表盘技能页双区升级 |
| 单元测试 | `tests/unit/skills.test.js` 8 例全绿（独立临时 DB，无污染） |
| 回归测试 | `db-driver-chain.test.js` 3 例全绿（SQLite 驱动链未受影响） |
| 构建 | `npm run build` 通过（standalone 产物生成 `/api/skills/*` 路由 + 新 dashboard/skills 页） |
| E2E（真实 HTTP 全链路） | 创建→列表→使用强化(uses+1,conf=0.55)→详情→更新→删除→404→参数校验(400) 全部通过 |
| 文档 | README.md 切换为中文主文档（保留 README.en.md），新增「技能系统」章节，更新 CHANGELOG v0.5.70 |
| 版本 | package.json → 0.5.70 |
| 提交 | `6bd9de20`（feat(skills): 技能系统动态化 + 中文 README） |
| 推送 | `git push` 成功（master + tag v0.5.70，远端 Everything up-to-date） |
| 发行版 | GitHub Release v0.5.70 创建成功：https://github.com/lza6/9router-Max/releases/tag/v0.5.70 |

# 十、剩余风险与后续建议
- **已披露 MINOR**：E2E 中文显示经终端 GBK 乱码（服务正确 UTF-8，终端显示层问题）；`README.en.md` 第 498 行含英文技术示例中的 `.../v1/embeddings`（非路径缺陷）。
- **后续 P1（未做，建议下批次）**：账号价值评分路由（NewAPI `computeRouteValueScore` 思路）、流式无效响应抑制（`classifySSEDataLine` 思路）。
- **P2**：自动签到、WebDAV 备份、会话观测看板对齐。

# 十一、需要用户补充的信息
- 无阻塞项。若要继续 P1-2/P1-3，明说即可。
